1) Execution order:
Extract landmarks -> train model -> air_canvas

2) You'll need to create a venv (vitual python environment on your system), locate it locally using terminal and activate venv script before running the project.
